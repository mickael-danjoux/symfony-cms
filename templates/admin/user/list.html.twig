{% extends 'admin/base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <ol class="breadcrumb main-breadcrumb px-3 py-2">
        <li class="breadcrumb-item"><a href="{{ path('admin_user_list') }}">Utilisateurs</a></li>
    </ol>

    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between">
                        <span class="h3 mb-0">Pages</span>
                        <a href="{{ path('admin_user_add') }}" class="btn-add">Ajouter</a>
                    </div>
                    <div class="card-body">
                        <div class="dataTable">Chargement des données...</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script>const datatable = {{ datatable_settings(datatable) }};</script>
    {{ parent() }}
{% endblock %}
